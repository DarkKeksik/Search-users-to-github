import styled from 'styled-components'

export const MainLayout = styled.div`
  display: grid;
  grid-template-columns: minmax(400px, .8fr ) 1.2fr;
  max-width: 1024px;
  width: 100%;
  margin: 0 auto;
  min-height: 100vh;
  background: #feebfe;
  box-sizing: border-box;
`